internal class Program
{
    private static void Main(string[] args)
    {
        string secretMessage = "Вы угадали пароль!";
        int triesCount = 3;
        string password = "12345";
        string userInput;
        int numberRemainingAttempts;

        Console.WriteLine("Отгадайте пароль чтобы увидеть секретное сообщение. У Вас 3 попытки.");

        for (int i = 0; i < triesCount; i++)
        {
            Console.Write($"Введите пароль: ");
            userInput = Console.ReadLine();
            numberRemainingAttempts = i + 1;

            if (userInput == password)
            {
                Console.WriteLine(secretMessage);
                break;
            }
            else
            {
                Console.WriteLine("Вы ввели не верный пароль! Попробуйте еще раз.");
                Console.WriteLine($"У Вас осталось " + (triesCount - numberRemainingAttempts) + " попыток.");
            }
        }

        Console.ReadKey();
